const dbconfig = {
    host: 'localhost',
    user: 'root',
    password: '',
    database: 'UrlShort'
}

const table = 'urls'

const mainDomain = 'something.io/'

module.exports = { dbconfig: dbconfig, mainDomain: mainDomain, table: table }